<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Upload Document - OliveStore</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <%- include('partials/navbar', { user }) %>
  <link rel="stylesheet" href="/css/stores.css" />
  <main class="container stores">
  <h1>Upload Store Document</h1>
  <% if (error) { %><p style="color:red"><%= error %></p><% } %>
  <main>
    <form method="post" enctype="multipart/form-data" action="/stores/<%= storeId %>/documents">
      <label>Document Type</label>
      <select name="docType" required>
        <option value="gst_certificate">GST Certificate</option>
        <option value="fssai_license">FSSAI License</option>
        <option value="shop_photo">Shop Photo</option>
        <option value="other">Other</option>
      </select>
      <label>Document File</label>
      <input type="file" name="document" accept="image/png,image/jpeg,application/pdf" required />
      <button type="submit">Upload</button>
    </form>
  </main>
</body>
</html>


